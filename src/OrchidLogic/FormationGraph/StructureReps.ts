/**
 * A StructureRep is the representation of formations and their
 * constituents (features, feature groupings, etc) in a convenient
 * json format
 */
export interface StructureRep {
    type: string; // What structure this represents, ie feature, formation, grouping
}