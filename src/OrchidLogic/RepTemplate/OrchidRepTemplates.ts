import { RepTemplate } from "./RepTemplate";

/**
 * These are the RepTemplates corresponding to the
 * major structures in Orchid, like the various formations,
 * features, feature groupings, etc.
 */
export const orchidRepTemplates: { [key: string]: RepTemplate; } = {

};